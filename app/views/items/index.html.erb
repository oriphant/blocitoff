<h1>List of My To Do Items</h1>
</br>
</br>
  <div class="row">
    <div class="col-md-12">
      <% @items.each_with_index do |item, i| %>  <!-- User each_with_index to create numbering -->
        <div class="media">  
          <div class="media-body lead">  
            <%=(i+1)%>)  <%= item.name%> <span class="pull-right"><%= link_to "Delete | ", item, method: :delete, data: {confirm: 'Are you sure you want to delete this To Do Item>'}%> 
              <!-- Could not update Status in the Items Table using Form_for construct.  Not sure why? -->
                <!-- http://stackoverflow.com/questions/11605358/rails-button-to-update-attribute-not-working -->
                <!-- http://stackoverflow.com/questions/7326125/ruby-rails-edit-attribute-using-a-button -->

              <%= link_to "Done", item_done_path(item) %>
            </span>
            </br>
            <small class="pull-right">Expires - <%= item.expiration.to_date.to_formatted_s(:long_ordinal)%></small>
          </br>
          </div>
        </div>
      <%end%>
    </div>
</div>

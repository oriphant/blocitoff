<h1>Add a New To Do Item</h1>
</br>

<%=form_for @item do |f| %>
      
      <div class="form-group">
        <%= f.label "Description" %>
        <%= f.text_field :name, class: 'form-control', placeholder: "Enter To Do Item" %>
      </div>
      <p>This item will expire 7 days from now, on <u><%=7.days.from_now.strftime("%B %d, %Y")%></u>.  It will be deleted permenantly 7 days after it expires <u><%=14.days.from_now.strftime("%B %d, %Y")%></u>.</p>
      </br>
      
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
<%end%>